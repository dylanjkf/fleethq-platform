# Deploy race probe A (R5 Critical #1)

Older commit in the forced-supersession test. Touches api/** to trigger api-ci
so deploy-api fires; expected to be SUPERSEDED once probe B lands on main first.
